"""Zorbeck — Deal-Alarm für Immobilien. Ein Angebot, ein Preis, ein Klick.

Separate Railway service (Root Directory = ``zorbeck``), independent of the
repo-root ``app`` package. Entry point stays ``app:app`` (see ``zorbeck/app.py``).
"""
