# No [build] section on purpose: Railway auto-detects the Dockerfile in each
# service's root directory. Naming it here forced the repo-root Python
# Dockerfile onto services rooted in subdirectories (nieczytaj/, liesnicht).

[deploy]
healthcheckPath = "/health"
restartPolicyType = "on_failure"
