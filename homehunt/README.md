# homehount.com — landing page collecting signups for property deal alerts (email + city + budget → SIGNUP_WEBHOOK_URL).
# Deploy: separate Railway service in this repo with Root Directory = `homehunt`; start command comes from the Procfile.
# Env: `SIGNUP_WEBHOOK_URL` (optional — without it signups are only logged).
